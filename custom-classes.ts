export const customClasses = {
  '.flex-center': { '@apply flex items-center justify-center': {}, },
  '.mobile-only': { '@apply sm:hidden': {}, },
  '.desktop-only': { '@apply max-sm:hidden': {}, },
}